# electron-deeplink-protocol
Electron desktop application demonstrating custom protocol handling (deep links). When a user clicks a hppo:// URL from a browser or external app, it opens this Electron app and passes data like authentication tokens. Built with Electron Forge and Vite.
